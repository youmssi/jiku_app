package com.jiku.shared

/**
 * Who an action on a ticket is attributed to when the organizer performs it from
 * their own account, rather than an operator through their link.
 */
const val ORGANIZER_OPERATOR_LABEL = "Organizer"
