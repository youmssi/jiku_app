spring:
  datasource:
    # No defaults: every value is required from the environment so a missing
    # one fails fast at startup instead of silently using a wrong database.
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
